# Type-O-Mancer: Realm Run

A static Vanilla JavaScript typing-race game. The project keeps campaign data
separate from map rendering and gameplay control, and has no network calls,
runtime configuration, or credentials. It deliberately uses isolated classic
scripts so the game also works when `index.html` is opened directly from disk.
